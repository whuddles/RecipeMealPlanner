@model List<WebApplication.Web.Models.Ingredient>

@{
    ViewData["Title"] = "GroceryList";
}

<h2>Grocery List</h2>


@for (int i = 0; i < Model.Count; i++)
{
    <p><span>@(i + 1)) </span><span>@Model[i].Name</span></p>
}